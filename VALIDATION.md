# 验证记录

2026-09-11，Windows 本机：

- 38 项自动化测试通过，覆盖当前值与历史独立、变化/周期/事件订阅、事件溢出、按键四种行为、四组 X 同步、像素抽稀、混合频率绘图与有界批次入口。
- 新增回归覆盖：使能/失能两个按键绑定同一变量，1/0 写入、订阅、事件、50 Hz 共享历史不重复采样、配置保存恢复、删除一个绑定者、滑块显示共享当前值。
- 删除波形底部内部状态说明及其布局占位，改动后完整 38 项测试仍通过。
- 原生界面先前已验证滑块更新到 0.51 后信号池与模拟跟随曲线响应、开关写入 1、配置对话框可打开。此次共享信号交互通过 Qt 组件测试验证。
- 模拟批次源的实收点率约 20 kHz；这不是硬件吞吐量或满负载绘图帧率测试。主机定时器频率受 Windows 调度影响。

未连接 MCU，未执行电机控制。EXE 使用 Qt 原生组件和 CPU 绘图，打包脚本包含 ICU 依赖检查。

最终 EXE 已通过 ICU 依赖审计和离屏启动/退出测试（退出码 0，无 errors.log）。
